<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      L'attribut d'encoche n'existe qu'à partir d'Android 9 : le déclarer ici
      évite un avertissement de compilation sur les versions antérieures, où
      il n'y a de toute façon pas d'encoche.
    -->
    <style name="Theme.Celeste" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/fond</item>
        <item name="android:colorBackground">@color/fond</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:colorAccent">@color/accent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
